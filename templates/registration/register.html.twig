{% extends 'base.html.twig' %} {% block title %}Register{% endblock %} {% block
body %}
<h1>Register</h1>

{{ form_start(registrationForm) }}
{{ form_row(registrationForm.email) }}
{{
    form_row(registrationForm.plainPassword, {
        label: 'Password'
    })
}}
{{ form_row(registrationForm.agreeTerms) }}

<<<<<<< HEAD
<button type="submit" class="btn">Register</button>
<p>
    Already have an account ?
    <a href="http://127.0.0.1:8000/login">Sign in</a>
</p>
{{ form_end(registrationForm) }}
=======
{% block content %}
        <div class="mx-auto max-w-2xl text-center">
            <h1 class="text-3xl font-bold tracking-tight text-gray-900 sm:text-4xl my-6" >Inscription</h1>
            <p class="mt-4 text-lg text-gray-600">Créez votre compte pour accéder à l'ensemble des fonctionnalités de l'application.</p>
            <h3>Vous avez déjà un compte ? <a href="{{ path('app_login') }}">Connectez-vous</a></h3>
            <hr class="my-4">
        </div>

        <div class="grid grid-cols-1 gap-x-8 gap-y-6 sm:grid-cols-2">
            {{ form_start(registrationForm) }}
            <span class="block text-sm font-semibold leading-6 text-gray-900"> 
                {{ form_row(registrationForm.email) }} 
            </span>

            <span class="block text-sm font-semibold leading-6 text-gray-900 mt-2.5"> 
            {{ form_row(registrationForm.plainPassword, {
                label: 'Password'
            }) }} 
            </span>

            <span class="block text-sm font-semibold leading-6 text-gray-900 mt-2.5"> 
                {{ form_row(registrationForm.agreeTerms) }}
            </span>
        </div>

        <button class="block rounded-md w-1/5 mx-auto bg-indigo-600 px-3.5 py-2.5 text-center text-sm font-semibold text-white shadow-sm hover:bg-indigo-500 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-2 focus-visible:outline-indigo-600" type="submit" class="btn">Register</button>
    {{ form_end(registrationForm) }}
>>>>>>> develop
{% endblock %}


