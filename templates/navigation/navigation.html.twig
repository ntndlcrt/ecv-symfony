<html>
    <div class="min-h-full">
        <nav class="bg-gray-800 py-4">
            <div class="ml-10 flex items-baseline space-x-4">
                <a href="{{ path('app_home') }}"<h1 class="text-3xl font-bold"> {{name}} </h1></a>
                    <div class="absolute right-0">
                        <a href="{{ path('app_home') }}" class=" text-gray-300 rounded-md px-3 py-2 text-sm font-medium" >Trouver un kdo</a>
                        <a href="{{ path('app_register') }}" class="text-gray-300 hover:text-white rounded-md px-3 py-2 text-sm font-medium ">Inscription</a>
                        <a href="{{ path('app_login') }}" class="text-gray-300 hover:text-white rounded-md px-3 py-2 text-sm font-medium ">Connexion</a>
                    </div>
            </div>
        </nav>
    </div>

</html>
